{
	"manifest_version": 2,
	
	"name": "URL Rewrite",
	"description": "An extension that lets you redirect Chrome for specific URLs - modified",
	"version": "1.0",

	"options_page": "options.html",

	"permissions": [
		"webRequest", "webRequestBlocking", "<all_urls>"
	],

	"background": {
		"scripts": ["background.js"]
	}

}